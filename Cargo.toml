[package]
name = "mnm"
version = "1.0.0"
edition = "2021"

[dependencies]
fips202 = { git = "https://github.com/fmerg/fips202-XOFs.git" }
serde_json = "1.0"
rand = "0.9.0"
regex = "1.11.1"

[profile.release]
# opt-level = 'z'
lto = true 
panic = 'abort'
strip = true